
# Hurtrock Music Store - Universal Port Configuration
# Format: KEY=VALUE
# Compatible dengan semua environment dan deployment

# Main Application Ports
FLASK_PORT=5000
DJANGO_PORT=8000

# Alternative Ports (jika port utama tidak tersedia)
ALT_FLASK_PORT=5001
ALT_DJANGO_PORT=8001

# Database Ports
POSTGRESQL_PORT=5432
REDIS_PORT=6379

# Development Mode
FLASK_DEBUG=0
DJANGO_DEBUG=0

# Host Configuration (universal binding)
FLASK_HOST=0.0.0.0
DJANGO_HOST=0.0.0.0

# Domain Configuration (tidak bergantung pada platform spesifik)
ALLOWED_DOMAINS=localhost,127.0.0.1,0.0.0.0

# SSL Configuration
SSL_ENABLED=false
SSL_CERT_PATH=
SSL_KEY_PATH=

# Production Settings
IS_PRODUCTION=false
USE_REVERSE_PROXY=false
